{
  "name": "web-porridge",
  "version": "0.3.0",
  "description": "Feature-enhanced wrappers for the WebStorage interface",
  "license": "MIT",
  "scripts": {
    "build": "tsc --pretty --project ./",
    "dev": "tsc --watch --pretty --project ./",
    "lint": "tslint --project ./",
    "lint:fix": "tslint --fix --project ./",
    "test": "ava ./test/web-porridge.js --verbose"
  },
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/idleberg/web-porridge.git"
  },
  "keywords": [
    "webstorage",
    "local-storage",
    "localStorage",
    "session-storage",
    "sessionStorage"
  ],
  "dependencies": {
    "dot-prop": "^5.2.0"
  },
  "devDependencies": {
    "@types/node": "^13.7.1",
    "ava": "^2.4.0",
    "browser-env": "^3.3.0",
    "husky": "^4.2.3",
    "localstorage-polyfill": "^1.0.1",
    "ts-loader": "^6.2.1",
    "tslint": "^6.0.0",
    "typescript": "^3.6.4",
    "webpack": "^4.41.6",
    "webpack-cli": "^3.3.11"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run lint"
    }
  }
}
